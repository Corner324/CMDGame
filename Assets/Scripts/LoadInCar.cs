using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadInCar : MonoBehaviour
{

    public int countPassengers;
    public int maxCount;

    // Start is called before the first frame update
    void Start()
    {
        countPassengers = 0;
        maxCount = 9;
    }

    // Update is called once per frame
    void Update()
    {
    }
}
